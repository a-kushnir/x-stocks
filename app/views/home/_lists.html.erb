<div class="small-widget my-side-panel mx-sm-auto">
  <h3>Lists</h3>
  <nav class="sidebar my-block">
    <ul class="nav flex-column">
      <% VirtualTag.all.each do |tag| %>
        <li class="nav-item">
          <a class="nav-link" href="<%= url_for(controller: 'stocks', action: 'index', tag: tag.name) %>"><%= tag.description %></a>
        </li>
      <% end %>
    </ul>
  </nav>
</div>
