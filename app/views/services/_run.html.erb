<% if current_user && (XStocks::Service.new.fast_update? || XStocks::Service.new.slow_update?) %>
  <div class="pr-4"
       data-controller="service"
       data-action="turbo:load@window->service#updateStocks">
    <span data-service-target="output"></span>
  </div>
<% end %>
