<%= render(DataTableComponent.new(name: render(partial: 'header'), table: @table, turbo_frame_id: 'datatable_stocks',
                                  bottom_left_block: link_to(new_stock_path, class: class_names('rounded-full bg-blue-500 text-white inline-block shadow-sm mt-1 p-1', 'float-left' => @table.pages.pages > 1), target: '_top') { inline_svg('svg/plus', size: '24*24') })) %>
